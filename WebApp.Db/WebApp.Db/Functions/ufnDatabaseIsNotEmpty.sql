CREATE FUNCTION [dbo].[ufnDatabaseIsNotEmpty]
(
)
RETURNS BIT

AS 

	BEGIN
		DECLARE @COUNT AS INT
		DECLARE @RESULT AS BIT

		SELECT @COUNT = COUNT(*)
		FROM INFORMATION_SCHEMA.TABLES

		IF(@COUNT > 1) 
			SET @RESULT = 1
		ELSE
			SET @RESULT = 0

		RETURN @RESULT
	END
